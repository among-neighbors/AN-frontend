const tableList = [];

const TableNav = () => {
  // redux는 어디서 처리해야하는가?
  // tableNavIndex 라는 값으로 하고, 클릭하면 이 값이 바뀌는걸로 하자.
  // 근데 변수 하나로 처리하면 페이지 이동할때마다 처리해줘야하잖아.
  // 차라리 store를 객체형태로 처리하자.

  return <></>;
};

export default TableNav;
